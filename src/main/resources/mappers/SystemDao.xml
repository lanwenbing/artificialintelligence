<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.artificialintelligence.dao.machinelearning.SystemDao">

    <!-- 查询一个系统 -->
    <select id="querySystemsByAlgorithmId" parameterType="int" resultType="com.artificialintelligence.machinelearning.model.SystemModel">
        select id, name,  description, location, algorithm_id as algorithmId,
        create_tm as createTm, modify_tm as modifyTm from material where algorithm_id=#{id}
    </select> 
    
    <!-- 查询一个系统 -->
    <select id="querySystemBySystemId" parameterType="int" resultType="com.artificialintelligence.machinelearning.model.SystemModel">
        select id, name,  description, location, algorithm_id as algorithmId,
        create_tm as createTm, modify_tm as modifyTm from material where id=#{id}
    </select>
</mapper>